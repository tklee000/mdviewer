# CEF 기반 Markdown Viewer/Editor 개발 계획

## 1. 프로젝트 목표

Windows 탐색기에서 `.md` 파일을 더블클릭하면 실행되어 Markdown 원문과 렌더링된 문서를 모두 확인하고 편집할 수 있는 데스크톱 프로그램을 개발한다.

첫 버전은 **Visual Studio 2019 16.11 / C++20 / Windows**를 기준으로 구현하되, 운영체제 종속 코드를 분리하고 UI를 CEF(Chromium Embedded Framework) 위에 구성하여 이후 Linux와 macOS로 이식할 수 있도록 한다. 현재 고정한 CEF 151 헤더가 C++20 concepts를 사용하므로 VS2019의 `/std:c++20` 모드를 적용한다.

### 핵심 사용 흐름

1. 사용자가 탐색기에서 `.md` 파일을 더블클릭한다.
2. 프로그램이 파일을 UTF-8 중심으로 읽어 연다.
3. 사용자는 메인 메뉴 또는 단축키로 다음 편집 방식을 전환한다.
   - **소스 편집**: Markdown 원문을 직접 편집한다.
   - **미리보기 편집**: 렌더링된 문서를 보면서 직접 편집한다.
4. 두 편집 방식의 내용은 동일한 문서 모델로 동기화된다.
5. 저장 시 Markdown 텍스트가 원래 파일에 안전하게 기록된다.

---

## 2. 1차 버전 범위

### 반드시 구현할 기능

- `.md`, `.markdown` 파일 열기
- 새 문서 만들기, 다른 이름으로 저장, 저장, 닫기
- 한국어를 포함한 12개 언어의 UI와 실행 중 언어 전환
- MiniGitGUI 계열 색상 토큰을 사용한 밝은 테마/다크 테마와 실행 중 전환
- Markdown 소스 편집
- 렌더링된 미리보기 화면에서의 편집(WYSIWYG 방식)
- 메인 메뉴에서 소스 편집/미리보기 편집 전환
- 수정 여부 표시 및 저장하지 않은 문서 닫기 방지
- 실행 파일 인자로 전달된 Markdown 파일 열기
- Windows 탐색기의 `.md` 파일 연결 및 더블클릭 실행
- 기본 편집 기능
  - 실행 취소/다시 실행
  - 잘라내기/복사/붙여넣기
  - 전체 선택
  - 찾기/바꾸기
- 제목, 목록, 링크, 이미지, 코드 블록, 표, 인용문 등 주요 Markdown 문법 지원
- UTF-8/UTF-8 BOM 인식 및 한글 파일명·본문 지원
- 외부에서 파일이 변경된 경우 충돌 알림
- 오류 로그와 비정상 종료 복구를 위한 임시 저장 기반 마련

### 1차 버전에서 제외할 기능

- 여러 사용자의 동시 편집
- Git 클라이언트 기능
- 클라우드 동기화
- 플러그인 마켓 또는 확장 기능 API
- PDF/HTML 내보내기
- 모바일 버전
- 완전한 CommonMark 확장 문법의 무손실 WYSIWYG 변환 보장

제외 기능은 기본 편집기가 안정화된 뒤 별도 단계로 검토한다.

---

## 3. 권장 기술 구성

| 영역 | 권장 기술 | 역할 |
|---|---|---|
| 네이티브 애플리케이션 | C++20, Visual Studio 2019 16.11 | 프로세스, 창, 파일, 메뉴, OS 통합 |
| 빌드 | CMake + VS2019 Generator | Windows 빌드 및 향후 Linux/macOS 빌드 공통화 |
| 웹 런타임 | CEF | 편집 UI와 Markdown 렌더링 실행 |
| 소스 편집기 | CodeMirror 6 계열 | 구문 강조, 검색, 커서/선택 영역, 편집 |
| 미리보기 편집기 | ProseMirror 계열 또는 검증된 Markdown WYSIWYG 편집기 | 렌더링 상태의 직접 편집 |
| Markdown 파서 | CommonMark/GFM 호환 JavaScript 라이브러리 | Markdown ↔ 편집기 문서 모델 변환 |
| 다국어 | 공용 JSON 메시지 카탈로그 + C++/JavaScript 로더 | 네이티브 메시지와 CEF UI의 동일한 번역 제공 |
| UI 코드 | HTML, CSS, TypeScript/JavaScript | CEF 내부 화면과 편집 동작 |
| 테스트 | GoogleTest + JavaScript 단위 테스트 도구 | C++ 코어와 편집 변환 로직 검증 |
| 설치 패키지 | WiX Toolset 또는 Inno Setup | 설치/제거 및 파일 연결 등록 |

### 기술 선정 원칙

- CEF 버전은 VS2019에서 빌드 가능한 배포판을 선정하고 프로젝트에 버전을 고정한다.
- CEF 바이너리를 저장소에 직접 넣기보다는 의존성 설치 스크립트 또는 별도 패키지로 관리한다.
- C++와 웹 UI 간 통신은 CEF 메시지 라우터를 감싼 좁은 인터페이스로 제한한다.
- 화면 문자열을 코드에 번역문으로 직접 넣지 않고 영어 원문 fallback과 locale별 JSON 카탈로그로 관리한다.
- UI 라이브러리 및 Markdown 파서는 라이선스, 유지보수 상태, GFM 지원 여부, HTML 처리 정책을 검토한 뒤 확정한다.
- 사용자 Markdown에 포함된 스크립트는 실행하지 않으며, 렌더링 HTML을 반드시 정제한다.

---

## 4. 전체 아키텍처

```text
┌───────────────────────────────────────────────┐
│ Native Application (C++20)                    │
│                                               │
│ App/Window ─ Document Service ─ File Service  │
│      │              │               │         │
│      │              │          Platform Layer │
│      │              │          Win/Linux/mac  │
│      └──── CEF Bridge / Command Dispatcher ───┤
└───────────────────────┬───────────────────────┘
                        │ JSON 메시지
┌───────────────────────▼───────────────────────┐
│ CEF Web UI                                    │
│                                               │
│ Menu State / Toolbar / Status                 │
│      │                                        │
│ Source Editor ⇄ Canonical Markdown ⇄ WYSIWYG  │
│      │                                        │
│ Markdown Parser / Sanitizer / Asset Resolver  │
└───────────────────────────────────────────────┘
```

### 계층별 책임

#### 네이티브 C++ 계층

- 애플리케이션 수명주기 및 CEF 초기화/종료
- 메인 창과 네이티브 메뉴 관리
- 명령행 파일 경로 처리
- 파일 열기/저장, 인코딩과 줄바꿈 처리
- 최근 파일, 사용자 설정, 자동 저장 파일 관리
- 선택 언어 저장, 네이티브 메뉴 재구성, 시작/파일 오류 메시지 번역
- 선택 테마 저장과 Windows 제목 표시줄 테마 적용
- 파일 변경 감시
- Windows 파일 연결과 단일 실행 인스턴스 처리
- Linux/macOS 이식 시 필요한 OS별 구현 분리

#### CEF 브리지

- C++ 명령을 웹 UI 명령으로 변환
- 웹 UI의 편집 내용과 상태를 C++에 전달
- 메시지 스키마 버전 관리
- 허용된 명령만 처리하는 검증 계층 제공

예상 메시지 예시는 다음과 같다.

```json
{ "type": "document.opened", "id": "...", "text": "# 제목", "path": "..." }
{ "type": "document.changed", "id": "...", "revision": 12, "text": "..." }
{ "type": "command.execute", "name": "file.save" }
{ "type": "editor.modeChanged", "mode": "source" }
{ "type": "settings.languageChanged", "locale": "ko-KR" }
```

#### 웹 UI 계층

- 소스 편집기와 미리보기 편집기 생성 및 상태 관리
- Markdown 렌더링과 WYSIWYG 변환
- 편집 모드 전환 시 커서 위치와 스크롤 위치 최대한 유지
- 문법 강조, 검색/바꾸기, 편집용 툴바 제공
- locale 카탈로그 로딩, 동적 UI와 접근성 텍스트의 즉시 재번역
- CSS 색상 토큰 기반 밝은/다크 테마 전환
- 웹 콘텐츠 정제 및 외부 링크/로컬 이미지 요청 제어

---

## 5. 문서 모델과 양방향 편집 전략

### 기준 데이터

저장되는 **Markdown 텍스트를 문서의 최종 기준 데이터**로 사용한다. C++ `Document` 객체는 다음 정보를 가진다.

- 문서 ID
- 파일의 절대 경로
- 현재 Markdown 텍스트
- 저장된 마지막 버전의 해시 또는 revision
- 변경 여부(dirty)
- 문자 인코딩과 BOM 여부
- 줄바꿈 방식(CRLF/LF)
- 마지막 수정 시각
- 소스/미리보기 편집 모드

### 편집 모드

#### 소스 편집 모드

- Markdown 원문을 직접 편집한다.
- 일정 시간의 debounce 후 렌더링용 문서 모델을 갱신한다.
- 구문 오류가 있어도 원문을 임의로 수정하거나 삭제하지 않는다.

#### 미리보기 편집 모드

- 렌더링된 제목, 문단, 목록, 표, 링크 등을 직접 편집한다.
- 편집기 내부 문서 모델을 Markdown으로 직렬화해 기준 텍스트를 갱신한다.
- 지원하지 않는 문법은 보호 블록 또는 읽기 전용 블록으로 표시하여 조용히 손실되지 않게 한다.

### 전환 규칙

1. 현재 편집기의 변경 내용을 먼저 확정한다.
2. revision을 증가시키고 기준 Markdown을 갱신한다.
3. 대상 편집기에 새 revision을 반영한다.
4. 제목/블록 ID를 이용해 가능한 한 커서와 스크롤 위치를 복원한다.
5. 변환 과정에서 손실 가능성이 발견되면 사용자에게 경고하고 원문 백업을 보존한다.

### 초기 기술 검증 항목

WYSIWYG 편집은 Markdown 문법을 다시 생성하는 과정에서 공백, 들여쓰기, HTML, 확장 문법을 바꿀 수 있다. 본 개발에 앞서 다음 샘플의 왕복 변환(round-trip) 테스트를 먼저 수행한다.

- 중첩 목록과 체크박스
- 표 정렬
- fenced code block과 언어명
- 인라인 HTML 및 HTML 블록
- 상대 경로 이미지와 링크
- 각주, 취소선, task list 등 GFM 확장
- front matter(YAML)
- 수식, Mermaid 등 향후 확장 후보
- 한글과 이모지

검증 결과에 따라 다음 중 하나를 확정한다.

1. 지원 문법은 완전 편집하고 미지원 문법은 보호 블록으로 유지한다.
2. 미리보기 편집 모드 진입 시 손실 가능 문법을 알려주고 사용자 확인 후 변환한다.
3. 필요한 경우 AST에 원문 구간 정보를 보존하는 사용자 정의 변환 계층을 추가한다.

---

## 6. 화면 및 메뉴 구성

### 메인 화면

```text
┌ 파일  편집  보기  도움말 ───────────────────────────┐
│ [소스 편집] [미리보기 편집]       문서명 *           │
├──────────────────────────────────────────────────────┤
│                                                      │
│               현재 모드의 편집 화면                  │
│                                                      │
├──────────────────────────────────────────────────────┤
│ Ln 12, Col 4 | Markdown | UTF-8 | CRLF | 저장 상태   │
└──────────────────────────────────────────────────────┘
```

### 메인 메뉴

- **파일**
  - 새 문서
  - 열기
  - 최근 파일
  - 저장
  - 다른 이름으로 저장
  - 닫기
  - 종료
- **편집**
  - 실행 취소/다시 실행
  - 잘라내기/복사/붙여넣기
  - 찾기/바꾸기
  - 전체 선택
- **보기**
  - 소스 편집
  - 미리보기 편집
  - 테마
    - 밝은 테마
    - 어두운 테마
  - 언어
    - English
    - 한국어
    - 日本語
    - Français
    - Deutsch
    - Русский
    - 简体中文
    - 繁體中文
    - Español
    - Português (Brasil)
    - हिन्दी
    - Bahasa Indonesia
  - 툴바 표시
  - 상태 표시줄 표시
  - 확대/축소/초기화
- **도움말**
  - 단축키
  - 버전 정보
  - 오픈소스 라이선스

### 기본 단축키

| 기능 | 단축키 |
|---|---|
| 새 문서 | `Ctrl+N` |
| 열기 | `Ctrl+O` |
| 저장 | `Ctrl+S` |
| 다른 이름으로 저장 | `Ctrl+Shift+S` |
| 찾기 | `Ctrl+F` |
| 바꾸기 | `Ctrl+H` |
| 소스/미리보기 편집 전환 | `Ctrl+Shift+M` |
| 확대/축소 | `Ctrl++` / `Ctrl+-` |

단축키는 CEF에 전달하기 전에 네이티브 계층에서 공통 명령으로 해석해 두 편집 모드에서 동일하게 동작하도록 한다.

### 밝은 테마/다크 테마

`D:\ai_works\minigitgui`의 테마 적용 방식을 따라 CSS 변수 기반의 공용 색상 토큰을 사용한다.

- `:root`는 다크 테마를 기본값으로 두고 배경, 표면, 경계선, 본문, 보조 텍스트, 강조색, 링크, 코드 영역, 그림자 토큰을 정의한다.
- `:root[data-theme="light"]`에서 같은 토큰을 밝은 팔레트로 재정의한다.
- 소스 편집기, 미리보기 편집기, 제목, 목록, 표, 인용문, 코드 블록, 보호 블록, 툴바와 상태 표시줄에 같은 토큰을 적용한다.
- 툴바의 테마 버튼과 `보기 > 테마 > 밝은 테마/어두운 테마` 메뉴에서 실행 중 전환한다.
- C++ 설정의 `theme=light|dark`를 기준 상태로 사용하고 `localStorage`는 첫 화면 깜박임을 줄이기 위한 캐시로만 사용한다.
- UI가 `settings.themeChanged` 메시지를 보내면 C++가 값을 검증하고 설정을 원자적으로 저장한 뒤 `theme.changed`를 다시 전달한다.
- Windows에서는 가능한 버전에서 DWM immersive dark mode를 사용해 제목 표시줄도 현재 테마와 맞춘다.
- 테마 전환은 Markdown 본문, dirty 상태, 커서, selection, 스크롤, undo/redo 기록을 변경하지 않는다.
- 첫 실행 기본값은 MiniGitGUI와 동일하게 다크 테마로 하고, 이후 사용자가 선택한 값을 복원한다.

---

## 7. 다국어 설계

### MiniGitGUI 참고 사항과 적용 방향

`D:\ai_works\minigitgui`에서 확인한 다국어 구조를 이 프로젝트의 기준 설계로 사용한다.

- 영어 UI 원문을 메시지 키이자 최종 fallback으로 사용한다.
- 영어는 별도 JSON 없이 소스 문자열을 사용하고, 다른 언어는 `locales/<locale>.json`으로 관리한다.
- 같은 JSON 카탈로그를 CEF 웹 UI와 네이티브 C++ 오류/대화상자가 함께 사용한다.
- `{name}` 형태의 이름 있는 매개변수 치환을 지원한다.
- 정적 HTML은 `data-i18n`, `data-i18n-title`, `data-i18n-aria-label`, `data-i18n-placeholder` 속성으로 번역한다.
- 동적으로 생성되는 문자열은 `t(message, parameters)` 형태의 API를 통해서만 표시한다.
- locale JSON과 웹 UI 리소스는 Release 실행 파일 또는 앱 리소스에 포함하고, CEF의 가상 HTTPS origin에서 제공한다.
- 개발 빌드는 환경 변수로 원본 `web/` 디렉터리를 직접 읽을 수 있게 하여 번역과 UI 수정 결과를 빠르게 확인한다.
- 선택 언어는 UTF-8 사용자 설정 파일에 저장하고 다음 실행에서 복원한다.
- 앱이 제공하는 언어에 해당하는 CEF locale `.pak` 파일만 배포물에 포함한다.

영어 문장이 키인 방식은 영어 문구 변경이 번역 키 변경으로 이어질 수 있으므로, 카탈로그 검증 도구로 키 추가·삭제·변경을 검사하고 번역 갱신 없이 영어 키를 변경하지 않는 규칙을 둔다. 규모가 커지면 의미 기반의 안정적인 메시지 ID로 이전할 수 있도록 `LocalizationService` 뒤에 숨긴다.

### 1차 지원 언어

| Locale | 언어 표시명 | CEF locale pack |
|---|---|---|
| `en-US` | English | `en-US.pak` |
| `ko-KR` | 한국어 | `ko.pak` |
| `ja-JP` | 日本語 | `ja.pak` |
| `fr-FR` | Français | `fr.pak` |
| `de-DE` | Deutsch | `de.pak` |
| `ru-RU` | Русский | `ru.pak` |
| `zh-CN` | 简体中文 | `zh-CN.pak` |
| `zh-TW` | 繁體中文 | `zh-TW.pak` |
| `es-ES` | Español | `es.pak` |
| `pt-BR` | Português (Brasil) | `pt-BR.pak` |
| `hi-IN` | हिन्दी | `hi.pak` |
| `id-ID` | Bahasa Indonesia | `id.pak` |

언어 선택 목록의 언어명은 현재 UI 언어로 번역하지 않고 각 언어의 고유 표기명을 사용하여 사용자가 잘못 선택한 상태에서도 다시 찾기 쉽게 한다.

### Locale 결정과 fallback

1. 저장된 사용자 설정이 있으면 해당 locale을 사용한다.
2. 첫 실행이면 OS UI locale을 정규화해 지원 목록과 비교한다.
3. 지원하지 않는 OS locale이면 `ko-KR`을 초기값으로 사용한다.
4. 선택 locale의 메시지가 없으면 영어 원문을 사용한다.
5. 카탈로그 로딩 자체가 실패하면 영어로 안전하게 표시하고 로그를 남긴다.

locale 정규화 규칙에는 다음 항목을 포함한다.

- `en` → `en-US`, `ko` → `ko-KR`, `ja` → `ja-JP`
- `zh-Hans`, 중국 본토 계열 → `zh-CN`
- `zh-Hant`, `zh-TW`, `zh-HK` 계열 → `zh-TW`
- `pt` 계열 중 별도 지원 locale이 없으면 `pt-BR`
- 대소문자 또는 `_`/`-` 차이를 정규화한 뒤 비교

C++ 설정값을 언어 상태의 기준으로 삼고 브라우저 `localStorage`는 초기 화면 깜박임을 줄이는 캐시로만 사용한다. 두 값이 다르면 C++ 설정을 우선한다.

### 실행 중 언어 전환

1. 사용자가 `보기 > 언어`에서 locale을 선택한다.
2. 웹 UI 또는 네이티브 메뉴가 `settings.languageChanged` 명령을 보낸다.
3. C++가 locale을 지원 목록으로 검증하고 설정 파일에 원자적으로 저장한다.
4. 네이티브 메뉴, 파일 대화상자 제목, 알림 문구를 새 언어로 다시 구성한다.
5. CEF UI가 새 카탈로그를 로드하고 정적 요소와 현재 표시 중인 동적 요소를 다시 렌더링한다.
6. 문서 텍스트, selection, undo 기록, 스크롤 위치는 변경하지 않는다.

앱 UI는 재시작 없이 전환한다. Chromium 자체 문자열의 locale은 CEF 시작 시 선택 언어로 지정한다. Release에서는 기본 브라우저 메뉴와 오류 페이지 노출을 제한하므로, CEF 내부 문자열 때문에 재시작을 요구하지 않도록 한다.

### 네이티브와 웹의 공용 번역 규칙

- C++ `LocalizationService::Text()`와 JavaScript `i18n.t()`는 동일한 fallback 및 매개변수 규칙을 사용한다.
- 치환 매개변수는 문자열을 이어 붙여 만들지 않고 `{path}`, `{name}`, `{count}`처럼 이름을 부여한다.
- 번역문에서 매개변수 순서를 자유롭게 바꿀 수 있어야 한다.
- 복수형이 필요한 문구는 단순 문자열 결합을 피하고, 필요 시 locale별 복수형 규칙 또는 `Intl.PluralRules`를 도입한다.
- 날짜, 시간, 숫자는 `Intl.DateTimeFormat`, `Intl.NumberFormat` 등 locale API를 사용한다.
- Markdown 문서 본문, 코드, 파일 경로는 절대 번역하지 않는다.
- `data-i18n-html`은 신뢰된 앱 카탈로그와 허용된 태그에만 제한하고, 일반 번역은 `textContent`를 사용한다.
- 단축키 자체는 번역하지 않되 메뉴 표시는 현재 플랫폼 표기 관례를 따른다.

### 패키징과 리소스 제공

- Windows에서는 locale JSON을 `RCDATA`로 실행 파일에 포함하여 C++ 시작 오류도 CEF 생성 전부터 번역할 수 있게 한다.
- 동일한 내장 JSON을 `UiResourceProvider`가 `locales/<locale>.json` 경로로 CEF에 제공한다.
- Linux/macOS에서는 동일한 `ILocaleResourceProvider` 계약으로 앱 bundle/resource 디렉터리에서 읽는다.
- CMake 목록은 앱 카탈로그와 CEF `.pak` 파일 목록을 한 곳에서 정의해 누락을 방지한다.
- 배포 테스트에서 지원 locale마다 JSON, CEF `.pak`, 폰트 fallback, 언어 선택 항목의 존재를 검사한다.

### 레이아웃과 접근성

- 독일어·러시아어처럼 문자열이 길어져도 잘리지 않도록 고정 너비 버튼을 피하고 유연한 레이아웃을 사용한다.
- 한중일 글꼴, 키릴 문자, 데바나가리 문자를 포함하는 시스템 글꼴 fallback을 정의한다.
- 번역 시 `title`, `aria-label`, `placeholder`, 빈 상태, 오류 메시지도 함께 갱신한다.
- `document.documentElement.lang`을 현재 locale로 설정한다.
- 현재 지원 언어는 모두 LTR이지만 향후 RTL locale을 위해 `dir` 속성과 논리 CSS 속성 사용을 고려한다.
- 편집기 콘텐츠 언어는 앱 UI locale과 분리하고, 문서가 명시한 언어 또는 사용자 입력을 그대로 유지한다.

---

## 8. 파일 처리 설계

### 열기

- 명령행과 파일 선택 대화상자로 전달된 경로를 정규화한다.
- 기본 지원 인코딩은 UTF-8과 UTF-8 BOM으로 한다.
- UTF-16 또는 로컬 코드 페이지 파일은 감지 후 변환 여부를 사용자에게 알린다.
- 상대 경로 이미지와 링크의 기준은 현재 Markdown 파일이 있는 디렉터리로 한다.
- 파일 크기 제한과 대용량 파일 경고 기준을 설정한다.

### 저장

- 같은 디렉터리에 임시 파일을 생성한다.
- 쓰기와 flush가 성공하면 원본을 교체하는 원자적 저장 방식을 사용한다.
- 기존 BOM과 줄바꿈 형식은 사용자가 변경하지 않는 한 유지한다.
- 저장 직전 외부 수정 여부를 다시 확인한다.
- 실패 시 원본 파일을 보존하고 구체적인 오류를 표시한다.

### 외부 변경 감시

- Windows에서는 `ReadDirectoryChangesW` 기반 구현을 우선 사용한다.
- Linux는 `inotify`, macOS는 `FSEvents` 또는 적절한 파일 감시 API로 어댑터를 구현한다.
- 문서가 수정되지 않은 상태면 자동 새로고침 여부를 설정으로 제공한다.
- 문서가 수정된 상태면 `디스크 버전 다시 불러오기 / 현재 내용 유지 / 다른 이름으로 저장` 선택을 제공한다.

### 자동 저장 및 복구

- 편집 중인 Markdown을 일정 간격으로 사용자 데이터 디렉터리에 임시 저장한다.
- 정상 저장/종료 시 해당 복구 데이터를 제거한다.
- 비정상 종료 후 다음 실행에서 복구 가능 문서를 안내한다.

---

## 9. Windows `.md` 파일 연결

### 설치 프로그램 등록

- `.md`와 `.markdown` 확장자를 프로그램의 ProgID에 연결한다.
- 파일 아이콘과 `열기` 명령을 등록한다.
- 실행 명령은 반드시 파일 경로 따옴표를 포함한다.

```text
"C:\Program Files\MdViewer\MdViewer.exe" "%1"
```

- 사용자 선택을 존중하도록 설치 과정에서 파일 연결 옵션을 제공한다.
- 제거 시 프로그램이 생성한 등록 정보만 정리한다.
- Windows 기본 앱 정책 때문에 강제 기본 앱 지정은 하지 않고, 필요하면 기본 앱 설정 화면으로 안내한다.

### 실행 중 파일 열기

- 기본 동작은 단일 애플리케이션 인스턴스로 한다.
- 두 번째 실행의 파일 경로를 IPC로 기존 인스턴스에 전달한다.
- 초기 버전은 한 창에서 한 문서를 열고, 이후 탭 방식의 다중 문서로 확장할 수 있도록 `DocumentManager`를 둔다.
- 경로에 공백, 한글, `#`, `%` 등이 있는 경우를 테스트한다.

---

## 10. 크로스플랫폼 준비

운영체제 종속 기능은 다음 인터페이스 뒤에 둔다.

```cpp
class IPlatformWindow;
class IFileDialog;
class IFileWatcher;
class IInstanceCoordinator;
class ISystemIntegration;
class IUserDataPathProvider;
class ILocaleResourceProvider;
```

### 공통으로 유지할 부분

- 문서 모델과 명령 시스템
- CEF 브리지 메시지
- HTML/CSS/JavaScript 편집 UI
- Markdown 파서와 변환 테스트
- 설정 파일 형식
- 다국어 카탈로그, locale 정규화와 fallback 규칙
- 대부분의 CMake 설정

### OS별 구현이 필요한 부분

| 기능 | Windows | Linux | macOS |
|---|---|---|---|
| 창/메뉴 | Win32 | GTK 등 | Cocoa |
| 파일 연결 | Registry/Installer | `.desktop`, MIME | App bundle/UTType |
| 파일 감시 | ReadDirectoryChangesW | inotify | FSEvents |
| 단일 인스턴스 | Named Mutex/Pipe | lock + socket/DBus | NSRunningApplication 등 |
| 사용자 데이터 경로 | AppData | XDG | Application Support |

Windows 구현 안에 CEF 초기화나 문서 로직이 섞이지 않도록 초기 단계부터 디렉터리와 인터페이스를 분리한다.

---

## 11. 보안 원칙

- Markdown의 원시 HTML은 기본적으로 정제한다.
- `<script>`, 이벤트 핸들러, 위험한 URL 스킴을 차단한다.
- CEF의 Node.js 연동은 사용하지 않는다.
- 파일 접근은 네이티브 계층이 승인한 현재 문서 및 관련 리소스로 제한한다.
- `file://` 직접 접근 대신 검증 가능한 사용자 정의 스킴 또는 리소스 핸들러를 검토한다.
- 외부 링크는 앱 내부에서 임의 실행하지 않고 확인 후 기본 브라우저로 연다.
- CEF 디버깅 포트와 개발자 도구는 Release 빌드에서 비활성화하거나 명시적 설정으로만 허용한다.
- C++ ↔ JavaScript 메시지는 타입, 크기, 문서 ID, revision을 검증한다.

---

## 12. 권장 프로젝트 구조

```text
mdviewer/
├─ CMakeLists.txt
├─ cmake/
├─ docs/
│  ├─ architecture.md
│  └─ message-protocol.md
├─ src/
│  ├─ app/                 # 앱 수명주기, 명령, DocumentManager
│  ├─ core/                # 문서 모델, 저장 정책, 공통 로직
│  │  └─ localization/     # locale 정규화, 카탈로그, fallback
│  ├─ cef/                 # CEF 초기화, 핸들러, 메시지 브리지
│  ├─ platform/
│  │  ├─ windows/
│  │  ├─ linux/
│  │  └─ macos/
│  └─ main/
├─ web/
│  ├─ src/
│  │  ├─ editor/
│  │  ├─ markdown/
│  │  ├─ bridge/
│  │  ├─ i18n/
│  │  └─ ui/
│  ├─ locales/             # locale별 공용 JSON 카탈로그
│  ├─ tests/
│  └─ package.json
├─ tests/
│  ├─ cpp/
│  ├─ fixtures/
│  └─ roundtrip/
├─ installer/
│  └─ windows/
├─ scripts/
└─ third_party/
```

---

## 13. 개발 단계

### 단계 0: 기술 검증

목표는 본 개발 전에 가장 위험한 부분을 확인하는 것이다.

- VS2019에서 CEF 샘플 빌드 및 실행
- CMake 기반 Debug/Release 빌드 확인
- C++ ↔ JavaScript 양방향 메시지 송수신
- 소스 편집기에서 Markdown 렌더링
- 미리보기 편집기에서 Markdown으로 재직렬화
- 주요 문법 round-trip 결과 비교
- 한글 경로의 로컬 이미지 표시
- MiniGitGUI 방식의 공용 locale JSON 로딩 및 네이티브/웹 fallback 검증
- 12개 지원 언어의 CEF locale pack 가용성 확인
- 라이브러리 라이선스 검토

**완료 조건:** 편집기 조합과 지원/비지원 Markdown 문법 정책이 문서로 확정된다.

### 단계 1: 애플리케이션 골격

- CMake 프로젝트와 VS2019 솔루션 생성
- CEF 초기화, 메인 창, 종료 처리
- C++ 계층 디렉터리와 플랫폼 인터페이스 구성
- 정적 웹 UI 로딩
- 로깅, 설정, 사용자 데이터 경로 구현
- `LocalizationService`, `ILocaleResourceProvider`, locale 정규화 구현
- 선택 언어를 CEF 시작 설정과 웹 UI 초기 상태에 전달
- 선택 테마를 CEF URL 초기 상태와 앱 상태 메시지에 전달

**완료 조건:** 빈 편집 화면을 표시하는 Windows 실행 파일의 Debug/Release 빌드가 성공한다.

### 단계 2: 파일 및 문서 코어

- `Document`, `DocumentManager`, command 구조 구현
- 새 문서/열기/저장/다른 이름으로 저장
- UTF-8, BOM, CRLF/LF 처리
- dirty 상태와 닫기 확인
- 원자적 저장 및 오류 처리
- 명령행 파일 열기

**완료 조건:** 한글 경로를 포함한 Markdown 파일을 열고 수정하여 내용 손상 없이 저장할 수 있다.

### 단계 3: 소스 편집 모드

- 소스 편집기 통합
- Markdown 구문 강조
- 찾기/바꾸기, 실행 취소/다시 실행
- 편집 변경을 revision 기반으로 C++ 문서에 전달
- 커서 위치, 줄/열, 스크롤 상태 표시

**완료 조건:** 일반 텍스트 편집기 수준으로 Markdown 원문을 안정적으로 편집할 수 있다.

### 단계 4: 미리보기 편집 모드

- Markdown 렌더러와 sanitizer 통합
- WYSIWYG 편집기 통합
- Markdown ↔ 편집 문서 모델 변환
- 소스/미리보기 모드 전환
- 전환 시 selection/scroll 복원
- 미지원 문법 보호와 경고 UI
- 이미지 및 링크 경로 처리

**완료 조건:** 지원 문법으로 작성한 문서가 두 모드 사이에서 내용 손실 없이 반복 편집된다.

### 단계 5: 메뉴 및 데스크톱 통합

- 네이티브 메인 메뉴와 단축키
- 12개 언어 선택 메뉴, 실행 중 전환, 설정 저장
- 밝은/다크 테마 메뉴와 툴바 전환, 설정 저장
- 최근 파일과 드래그 앤 드롭 열기
- 단일 인스턴스와 파일 경로 IPC 전달
- 파일 변경 감시와 충돌 처리
- 자동 저장과 충돌 복구
- 아이콘과 Windows 설치 프로그램
- `.md`, `.markdown` 파일 연결

**완료 조건:** 설치 후 탐색기 더블클릭으로 파일이 열리고, 이미 실행 중인 경우에도 정확한 파일이 전달된다.

### 단계 6: 안정화 및 배포

- 단위/통합/수동 테스트 완료
- 대용량 파일 성능과 메모리 점검
- 고해상도 DPI, 다중 모니터, IME 한글 입력 검증
- CEF 프로세스 종료/충돌 동작 확인
- Release 서명 및 설치/업데이트 절차 정리
- 오픈소스 고지 파일 작성
- 12개 locale의 번역 누락, UI 잘림, JSON/CEF pack 패키징 검증
- 두 테마에서 Markdown 요소의 명암비, 포커스, 선택 영역, 코드 가독성 검증

**완료 조건:** 릴리스 체크리스트를 통과하는 설치 패키지와 사용자 안내 문서가 준비된다.

### 단계 7: Linux/macOS 이식

- 공통 코어의 운영체제 의존성 재점검
- 각 OS의 창, 메뉴, 대화상자, 파일 감시 구현
- 파일 연결/MIME/App bundle 설정
- 플랫폼별 locale 리소스 로더와 OS locale 감지 구현
- CI 빌드 추가
- 플랫폼별 설치 패키지와 서명/공증 절차 추가

---

## 14. 테스트 계획

### C++ 단위 테스트

- 문서 상태와 revision 변화
- 인코딩/BOM/줄바꿈 보존
- 원자적 저장의 성공 및 실패
- 외부 변경 탐지와 충돌 판단
- 명령행 및 IPC 경로 파싱
- locale 정규화, fallback, 매개변수 치환, 설정 저장/복원

### JavaScript 단위 테스트

- Markdown 파싱과 직렬화
- sanitizer 정책
- 소스/미리보기 상태 전환
- revision이 오래된 메시지 무시
- 지원/미지원 블록 판별
- 정적/동적 UI 번역, HTML 속성 번역, 언어 전환 후 편집 상태 보존

### 다국어 카탈로그 테스트

- 모든 locale JSON의 UTF-8/JSON 문법 검사
- 영어 fallback 키와 번역 키의 누락·불필요 키 비교
- 원문과 번역문 사이의 `{parameter}` 집합 일치 검사
- 대소문자만 다른 중복 키 및 실수로 변경된 영어 키 검사
- 각 locale에서 메뉴, 대화상자, 상태 표시줄, 오류 화면 스냅샷 확인
- 독일어/러시아어 장문, 중국어/일본어, 한글 IME, 힌디어 글리프 표시 확인
- 언어 전환 전후 문서 내용, dirty 상태, undo/redo, 커서와 스크롤 불변 확인
- 잘못되거나 누락된 카탈로그에서 영어 fallback과 오류 로그 확인

### 테마 테스트

- 툴바와 네이티브 메뉴에서 밝은/다크 테마를 각각 전환
- 앱 재시작 후 마지막 선택 테마 복원
- 소스 편집기와 미리보기의 제목, 링크, 표, 인용문, 인라인 코드, fenced code, 이미지 표시 확인
- 테마 전환 전후 문서 내용, dirty 상태, undo/redo, selection과 스크롤 불변 확인
- 12개 locale의 긴 메뉴 문자열과 두 테마 조합 확인
- Windows 10/11에서 제목 표시줄 다크 모드 지원 및 미지원 환경의 안전한 fallback 확인

### Round-trip 고정 샘플 테스트

```text
원본 Markdown
  → WYSIWYG 문서 모델
  → 편집 없음
  → Markdown 재생성
  → AST 의미 및 보호 영역 비교
```

공백 등 허용된 정규화와 실제 데이터 손실을 구분하여 검사한다. 사용자 작성 원문을 반드시 그대로 유지해야 하는 문법은 별도 fixture로 관리한다.

### 통합 테스트

- 메뉴 명령이 활성 편집기에 정확히 전달되는지 확인
- 저장하지 않은 변경 내용 보호
- 탐색기 더블클릭과 실행 중 두 번째 파일 열기
- 상대 경로 이미지 표시
- 외부 프로그램이 파일을 변경했을 때 충돌 UI
- 자동 저장 후 비정상 종료 복구

### 수동 호환성 테스트

- Windows 10/11
- 100%, 125%, 150%, 200% DPI
- 한글 Microsoft IME 입력과 조합 중 모드 전환 방지
- 12개 locale 선택과 앱 재시작 후 선택 언어 복원
- 네트워크 드라이브, 읽기 전용 파일, 긴 경로
- 공백/한글/특수문자가 포함된 경로
- 1 MB, 10 MB 이상의 Markdown 파일

---

## 15. 완료 기준

1차 Windows 버전은 다음 조건을 모두 충족해야 완료로 본다.

- VS2019 환경에서 문서화된 절차대로 Debug/Release 빌드가 된다.
- 설치 후 `.md` 파일 더블클릭으로 앱이 실행되고 해당 문서가 열린다.
- 소스 편집과 미리보기 편집을 메뉴 및 단축키로 전환할 수 있다.
- 지원하기로 한 Markdown 문법이 두 편집 모드 사이에서 의미 손실 없이 유지된다.
- 지원하지 않는 문법은 사용자 모르게 삭제되거나 변형되지 않는다.
- 한글 입력, 한글 경로, UTF-8 파일 열기/저장이 정상 동작한다.
- 12개 지원 언어가 메뉴에서 선택되고 재시작 후에도 유지된다.
- 네이티브 오류와 CEF UI가 동일한 locale을 사용하며, 누락된 번역은 영어로 안전하게 fallback된다.
- 모든 locale에서 주요 메뉴와 대화상자가 잘리지 않고 접근성 텍스트도 번역된다.
- 밝은/다크 테마가 전체 편집 화면에 적용되고 재시작 후 선택이 유지된다.
- 테마 전환으로 문서 내용이나 편집 상태가 변경되지 않는다.
- 저장 실패, 외부 변경, 미저장 종료 시 데이터가 보호된다.
- Debug/Release에서 CEF 하위 프로세스가 정상 시작되고 종료된다.
- 설치 제거 시 사용자 문서가 삭제되지 않는다.
- 자동화 테스트와 릴리스 체크리스트를 통과한다.

---

## 16. 주요 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| WYSIWYG 왕복 변환 시 Markdown 문법 손실 | 사용자 문서 훼손 | 기술 검증 선행, 보호 블록, 원문 백업, fixture 테스트 |
| CEF 배포 크기와 업데이트 복잡도 | 설치 파일 증가 | CEF 버전 고정, 의존성/패키징 자동화 |
| C++/JavaScript 상태 불일치 | 저장 내용 누락 | 문서 ID와 revision 기반 프로토콜, 저장 전 동기화 확인 |
| 원시 HTML 및 링크 보안 | 코드 실행/파일 노출 | sanitizer, 탐색 제한, 사용자 정의 리소스 핸들러 |
| 대용량 Markdown 편집 지연 | 사용성 저하 | debounce, 증분 렌더링, 크기 경고, 성능 테스트 |
| 한글 IME 조합 중 상태 전환 | 입력 유실 | composition 상태 감지, 전환/저장 시점 제어 |
| VS2019와 최신 프런트엔드 도구 호환성 | 빌드 불안정 | C++ 빌드와 웹 자산 빌드를 분리하고 버전 잠금 |
| OS별 네이티브 동작 차이 | 이식 비용 증가 | 플랫폼 인터페이스와 공통 테스트를 초기부터 유지 |
| 영어 문구 변경과 번역 카탈로그 불일치 | 일부 언어가 영어로 표시됨 | 빌드/CI 키 비교, 영어 키 변경 리뷰 규칙 |
| 긴 번역문과 글꼴 차이 | 버튼 잘림 또는 문자 깨짐 | 유연한 레이아웃, locale별 스냅샷, 시스템 폰트 fallback |
| 앱 locale JSON 또는 CEF `.pak` 누락 | 특정 언어 시작/표시 오류 | 단일 CMake locale 목록과 배포물 자동 검사 |
| 테마별 낮은 명암비 또는 하드코딩된 색상 | 일부 Markdown 요소를 읽기 어려움 | 색상 토큰 강제, 두 테마 스냅샷과 접근성 검사 |

---

## 17. 권장 우선순위 및 첫 산출물

첫 구현에서는 기능 수를 늘리기보다 다음 순서를 지킨다.

1. CEF가 포함된 최소 Windows 실행 파일
2. C++ ↔ JavaScript 메시지 프로토콜
3. 파일 열기/안전한 저장
4. 소스 편집
5. Markdown WYSIWYG 왕복 변환 검증
6. 소스/미리보기 편집 전환
7. 공용 다국어 카탈로그와 실행 중 언어 전환
8. 밝은/다크 테마와 설정 저장
9. 파일 연결과 설치 프로그램
10. 자동 저장, 외부 변경 감시, 안정화

기술 검증 단계의 첫 산출물은 아래와 같다.

- 빌드 가능한 VS2019/CMake 프로젝트 골격
- CEF 버전 및 다운로드/설치 방법 문서
- 소스 편집기와 WYSIWYG 편집기 비교 결과
- Markdown round-trip 테스트 샘플과 결과
- C++ ↔ JavaScript 메시지 프로토콜 초안
- 1차 지원 Markdown 문법 표
- MiniGitGUI 패턴을 반영한 `LocalizationService`/`i18n` 골격과 12개 locale 목록
- 번역 키·매개변수·CEF locale pack 검증 스크립트

이 산출물이 확정된 후 본 구현을 진행하면 미리보기 편집의 문법 손실 문제와 CEF/VS2019 호환성 문제를 초기에 줄일 수 있다.

---

## 18. Linux/macOS 이식 기반 구현 현황 (2026-08-20)

Windows에서 대상 운영체제 실행 검증은 할 수 없지만, 다음 단계에서 각 네이티브 개발 환경으로 프로젝트를 그대로 옮겨 빌드와 디버깅을 시작할 수 있도록 실행부와 패키징 골격을 작성했다.

### 공통 CEF/문서 계층

- `platform/common/PortableCef.*`: 렌더러 JavaScript 브리지, 프로세스 메시지, 가상 HTTPS 리소스 스킴, 탐색 제한
- `platform/common/PortableCore.*`: UTF-8/BOM/줄바꿈 보존, 임시 파일 기반 저장, dirty 상태, 열기/저장/종료 확인, 플랫폼 대화상자 계약
- Windows판과 동일한 `web/` 편집기, 12개 locale, 밝은/다크 테마를 그대로 사용
- Markdown 상대 이미지 요청은 현재 문서 디렉터리 내부로 제한

### Ubuntu 22.04 / AppImage

- `linux/src/main.cpp`: GTK3 프레임리스 창과 X11/XWayland CEF 자식 창, 파일 대화상자, 창 명령, OS locale/테마 감지
- `linux/CMakeLists.txt`: Linux CEF wrapper, GTK3, X11 링크와 AppDir 설치 레이아웃
- `.desktop`/MIME 메타데이터와 오렌지 `M` PNG 아이콘 포함
- `linuxdeploy`로 런타임 의존성을 수집하고 `appimagetool`로 x86_64 AppImage 생성
- 1차 테스트 기준은 Ubuntu 22.04 LTS이며 Wayland 세션에서는 XWayland를 사용

### macOS / DMG

- `macos/src/main_mac.mm`: Cocoa 창과 CEF 자식 뷰, Finder 문서 열기, 파일 대화상자와 창 명령
- 메인 앱과 CEF GPU/Renderer/Plugin Helper 앱 번들 생성
- `Info.plist`에 `.md`/`.markdown` 문서 형식과 Markdown UTI 등록
- CEF Framework, 편집기 리소스, 생성된 `.icns`를 `MdViewer.app` 안에 포함
- CEF JIT용 signing entitlements와 `hdiutil` 기반 DMG 생성 스크립트 포함

### 네이티브 환경에서 이어서 검증할 항목

1. 대상 OS용 CEF 배포판 버전을 Windows판 CEF 151 계열과 맞추고 최초 컴파일 오류 조정
2. Ubuntu 22.04에서 GTK 이벤트 루프, XWayland 창 이동/최대화, AppImage 내 공유 라이브러리 점검
3. macOS에서 CEF Framework rpath, 네 종류 Helper 실행, Finder open-event와 창 드래그 점검
4. Linux AppImage MIME 기본 앱 등록과 macOS 코드 서명·notarization을 실제 배포 인증서로 검증
5. 네이티브 대화상자 번역과 설정 파일 영속화를 Windows판 수준으로 확장
